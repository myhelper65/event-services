apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eventservice-chart.fullname" . }}-test-connection"
  labels:
    {{- include "eventservice-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "eventservice-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
